# Unified Survey System Environment Configuration

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=survey_db
DB_USER=survey_user
DB_PASSWORD=your_password_here

# Server Configuration
PORT=5000
NODE_ENV=production

# Session Configuration
SESSION_SECRET=your_super_secret_session_key_change_this_in_production

# Frontend URL (optional in unified mode, used for external deployments)
# FRONTEND_URL=http://localhost:3000

# Email Configuration (SendGrid)
# SENDGRID_API_KEY=your_sendgrid_api_key

# Hugging Face API Tokens (for AI features)
# HF_TOKEN_1=your_hugging_face_token_1
# HF_TOKEN_2=your_hugging_face_token_2

# Security
# CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info